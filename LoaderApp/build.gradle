buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.5.2'
    }
}

repositories {
    google()
}

apply plugin: 'com.android.application'

android {
    namespace "app.trev.naotloader"

    compileSdkVersion 34

    defaultConfig {
        targetSdk 34
    }
}
